spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/amena
    username: amena
    password: amena123

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.amena: DEBUG
    org.hibernate.SQL: DEBUG